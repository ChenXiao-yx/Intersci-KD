# InterSci-KD 证据与领域参考手册

本手册为 SKILL.md 提供全部数值规则与关键字映射的权威出处。AI 在执行蒸馏任务时，遇到具体数值或分类问题必须优先查阅本手册对应章节。

---

## §1 证据权重表（Evidence Weights）

完整 24 种证据类型及权重（与 `scripts/config/evidence_weights.json` 一致，评分器实际加载此表）。**域覆盖列**：该类型在 `domain_overrides` 中被某域覆盖后的实际权重（P0-3）；标注"同全局"表示覆盖值与全局一致（显式声明但无实际差异）；未标注则无覆盖、全局值即生效值。审计时以计分输出 `detailed_scores[].weight_source` 字段为准（`global` 或 `domain_override:<域>`）。

| 证据类型 | 权重 | 域覆盖 | 说明 |
| :--- | :--- | :--- | :--- |
| FDA_NMPA_approval | 5.0 | drug=5.0, regulatory=5.0（同全局） | 最高等级：NMPA/FDA 批准（监管批准进 core） |
| CE_mark | 5.0 | regulatory=5.0（同全局） | 最高等级：CE 认证（监管批准进 core） |
| RCT | 5.0 | drug=5.0（同全局） | 随机对照试验 |
| Guideline | 4.0 | regulatory=4.0（同全局） | 临床指南 |
| Industry_standard | 3.0 | hardware=3.0, material=3.0（同全局） | 行业标准/规范 |
| Patent | 3.0 | hardware=3.0（同全局） | 专利文献 |
| Diagnostic_accuracy | 3.0 | — | 诊断准确性研究 |
| Meta_analysis | 2.5 | drug=3.0 | drug 域 Meta 分析权重更高 |
| Systematic_review | 2.5 | — | 系统综述 |
| External_validation | 2.5 | — | 外部验证研究 |
| Cohort_study | 2.0 | — | 队列研究 |
| Case_control | 2.0 | — | 病例对照研究 |
| Literature_review | 1.5 | — | 普通综述文献（叙述性综述） |
| Prototype_implementation | 1.5 | hardware=2.0, material=2.0 | 硬件/材料域原型即功能验证，权重更高 |
| Benchmark_study | 1.5 | AI=2.0 | AI 域基准测试为主要证据形态，权重更高 |
| Regulatory | 1.0 | — | 非批准类监管文件（警告/召回/公告，进 supplemental） |
| Journal_article | 1.0 | — | 普通期刊论文 |
| Conference_paper | 1.0 | AI=1.5 | AI 域会议论文为主要证据形态，权重更高 |
| Animal_experiment | 1.0 | — | 动物实验 |
| Case_study | 1.0 | — | 病例研究/个案 |
| In_vitro_study | 1.0 | — | 体外实验/细胞实验 |
| Simulation | 0.5 | — | 仿真/模拟研究（in silico） |
| Expert_opinion | 0.5 | — | 专家意见/共识 |
| News_report | 0.5 | — | 新闻报道/媒体发布 |
| （未分类默认值） | 0.3 | — | 未识别类型的兜底权重 |

**知识图谱概念类型**（§13 工具映射引用，配置层不直接加载；落表计分前由 `normalize_type` 按 venue/DOI 归一化到上表标准类型）：

| 概念类型 | 文档权重 | 归一化去向 |
| :--- | :--- | :--- |
| PubMed_literature | 2.0 | Journal_article（或按 venue DOI 映射） |
| NCBI_database | 2.5 | External_validation / 对应研究类型 |
| ChEMBL_compound | 2.0 | Journal_article / 对应数据来源类型 |
| Algorithm_benchmark | 1.0 | Benchmark_study（1.5）或 Conference_paper（1.0） |

---

## §2 置信度乘数（Confidence Multipliers）

证据权重 × 置信度乘数 = 该条证据实际得分的乘数因子。

| 置信度等级 | 乘数 | 判定标准（4 类指标命中数） |
| :--- | :--- | :--- |
| high | 1.0 | ≥3 类指标：样本量描述 + 统计指标 + 显著结果 + 方法学描述 |
| medium | 0.5 | 命中 2 类指标：中等证据强度 |
| low | 0.25 | 命中 ≤1 类指标：或仅有标题/摘要，无数据字段 |

---

## §3 年份衰减与等级阈值

**年份衰减规则**（适用所有 ≥1 篇论文的证据）：

| 条件 | 衰减系数 |
| :--- | :--- |
| 距今 ≤5 年 | 1.0（不衰减） |
| 距今 >5 年 | 每多 1 年 -0.05，线性衰减；最低为 0.3 |
| 未提供年份 | decay_factor = 1.0，不做衰减 |

计算公式（距今 8 年示例）：`max(0.3, 1 - (8-5)*0.05) = max(0.3, 0.85) = 0.85`

**证据总分等级阈值**（满分 8.0，≥8.0 为强证据门槛）：

| 等级 | 阈值 | 人话标签 | 标签符号 |
| :--- | :--- | :--- | :--- |
| green（绿） | 总分 ≥ 8.0 | 强 | 🟢 |
| yellow（黄） | 4.0 ≤ 总分 < 8.0 | 中 | 🟡 |
| red（红） | 总分 < 4.0 | 弱 | 🔴 |

---

## §4 领域识别关键字表（Domain Identification）

本表供 AI 判断用户输入所属领域使用。判定流程：将输入中文本（全小写化）与下表关键词匹配。若命中 ≥2 个领域，则优先查看 §4.1 交叉组合。

| 领域键 | 关键词（中英文混合，≥10 条/领域，取核心子集） |
| :--- | :--- |
| biotech（药物/生物/医疗） | 癌症 tumor 基因 gene 靶点 突变 药物 抑制剂 inhibitor 化合物 compound 分子 血糖 glucose CGM 临床 clinical 抗体 antibody 疫苗 vaccine 免疫 mRNA CAR-T 生物标志物 biomarker 靶向治疗 临床试验 基因检测 基因治疗 罕见病 新药研发 |
| hardware（硬件/传感器/材料） | 传感器 sensor 可穿戴 wearable 设备 device 硬件 柔性 电极 PCB 嵌入式 embedded MEMS 芯片 circuit 纳米 3D打印 植入式 能量采集 energy harvesting 低功耗 computing 生物相容性 边缘计算 柔性电子 材料 material |
| social（社科/传播/短视频） | 视频 博主 传播 媒体 内容创作 完播率 用户留存 短视频 算法推荐 流量 私域 KOL UGC 社交 社区 增长 转化率 变现 品牌 直播 内容 用户增长 |
| education（教育/学习） | 教育 学习 培训 课程 知识 在线教育 自适应学习 认知诊断 个性化学习 学习分析 教学 教育科技 adaptive learning cognitive |
| business（商业/营销/电商） | 商业 市场 营销 电商 运营 盈利模式 GMV B2B B2C SaaS 商业模式 价值链 竞争优势 护城河 规模化 commercial profit |
| psychology（心理/情绪） | 心理 情绪 心理测评 心理咨询 焦虑 anxiety 抑郁 depression CBT 正念 mindfulness 认知行为 情绪调节 压力 stress 睡眠 成瘾 创伤 |
| AI（人工智能/大模型） | 人工智能 AI 大模型 LLM GPT RAG Agent 生成式AI Chatbot 端侧推理 小模型 多模态 算法 模型 机器学习 |

### §4.1 跨领域组合（Cross Combinations）优先匹配

| 组合 | 典型交叉方向 |
| :--- | :--- |
| drug + hardware | 生物传感器、CGM、可穿戴医疗 |
| AI + drug | AI 药物设计、虚拟筛选 |
| AI + hardware | 端侧推理、边缘 AI、AI 芯片 |
| AI + education | 自适应学习系统、AI 教育助手 |
| AI + psychology | 情绪识别 AI、数字心理健康 |
| AI + business | 商业智能 AI、预测性分析 |
| social + education | 知识短视频、教育传播 |
| social + business | 内容营销、社媒电商 |
| psychology + education | 校园心理健康、学习心理干预 |
| psychology + hardware | 心理信号可穿戴采集 |
| drug + psychology | 精神药物疗效、神经心理干预 |

---

## §5 领域证据权重复用映射表（domain_map）

新增领域时，优先复用现有领域的权重体系，避免重复配置。**完整映射见 `scripts/config/evidence_weights.json` 的 `domain_map` 段**，下表仅列示例；两者必须一致，修改时同步。

| 领域键 | 复用的权重域 | 说明 |
| :--- | :--- | :--- |
| biotech | biotech | 生物医药默认权重集 |
| social | social | 社会科学权重集 |
| business | business | 商业自有权重集 |
| hardware | hardware | 智能硬件自有权重集 |
| AI | AI | AI/ML 自有权重集 |
| drug | drug | 药物化学自有权重集 |
| regulatory | regulatory | 法规/监管自有权重集 |
| oncology | oncology | 肿瘤学自有权重集 |
| chemistry | chemistry | 化学自有权重集 |
| material | material | 新材料自有权重集 |
| patent | patent | 专利分析自有权重集 |
| knowledge | knowledge | 知识图谱自有权重集 |
| education | social | 教育复用社科权重集 |
| psychology | social | 心理复用社科权重集 |
| default | biotech | 默认兜底，复用硬科技权重集 |

> **注意**：未在 `domain_map` 中的 domain 会导致 `score_evidence.py` 的 `resolve_domain` 显式报错（`ValueError: Unknown domain`），不再静默回退 `biotech`。新增领域必须在 `domain_map` 中显式注册。

---

## §6 瓶颈匹配表（Bottleneck Matching，≥30 条）

输入命中关键词组合时，自动推断该交叉点的核心发展瓶颈（供第零章核心科学挑战和第七章冲突引用）。每条最多 40 字。

| 命中关键词 | 推断瓶颈 |
| :--- | :--- |
| 心理健康+青少年 | 青少年心理信号非侵入式采集与识别精度矛盾 |
| 心理健康+监测 | 心理客观指标与主观感受映射精度不足 |
| 情绪识别+生理 | 生理信号与情绪非线性映射导致误判率高 |
| 心理+教育 | 校园心理干预的伦理边界与数据隐私矛盾 |
| 糖尿病+饮食 | 食物 GI 值估算精度（烹饪方式差异大） |
| 减重+饮食 | 中餐热量估算误差常≥30%（成分复杂） |
| 营养+推荐 | 个性化推荐精度差，用户个体差异大 |
| 外卖+饮食 | 外卖数据获取难，商家合作意愿低 |
| CGM+传感器 | 柔性传感器信号漂移与长期稳定性矛盾 |
| CGM+可穿戴 | 柔性传感器信号漂移与长期稳定性矛盾 |
| CGM | CGM 设备成本高，用户接受度有限 |
| 传感器+可穿戴 | 可穿戴信号漂移与皮肤贴合稳定性 |
| 传感器 | 信噪比和稳定性要求高，校准周期长 |
| 可穿戴 | 续航与舒适度矛盾，用户佩戴意愿低 |
| AI+药物 | AI 药物设计模型泛化性不足，临床验证成本高 |
| 罕见病+药物 | 患者招募困难、临床终点设计复杂 |
| AI+医疗 | AI 医疗诊断需 FDA/NMPA 审批，合规门槛高 |
| 短视频 | 用户注意力碎片化与知识深度矛盾（完播率背离收藏率） |
| 视频 | 用户注意力碎片化与知识深度矛盾（完播率背离收藏率） |
| 传播 | 信息过载环境下高价值内容触达效率衰减（算法茧房） |
| 媒体 | 信息过载环境下高价值内容触达效率衰减（算法茧房） |
| 教育 | 标准化知识传授与个性化认知差异的鸿沟 |
| 学习 | 标准化知识传授与个性化认知差异的鸿沟 |
| 商业 | 用户生命周期价值（LTV）与获客成本（CAC）失衡风险 |
| 营销 | 用户生命周期价值（LTV）与获客成本（CAC）失衡风险 |
| 心理 | 主观自评数据与客观生理指标关联性弱（社会赞许偏差） |
| 情绪 | 主观自评数据与客观生理指标关联性弱（社会赞许偏差） |
| 博主 | 内容创作者持续产出与个人 IP 品牌损耗冲突 |
| 内容创作 | 内容创作者持续产出与个人 IP 品牌损耗冲突 |
| 知识付费 | 高完播率与低转化率的用户决策延迟现象 |
| 付费课程 | 高完播率与低转化率的用户决策延迟现象 |
| AI+药物 | AI 药物预测模型泛化性与样本量不足矛盾 |
| AI+教育 | 自适应学习系统个性化与教学标准化平衡 |
| AI+心理 | AI 情绪识别准确性与隐私保护矛盾 |
| AI+硬件 | 边缘 AI 推理的计算效率与模型精度的平衡 |
| 机器人+康复 | 康复机器人自主控制与安全约束平衡 |
| 基因治疗+罕见病 | 罕见病基因治疗疗效与脱靶效应矛盾 |
| 脑机接口+植入式 | 脑机接口信号稳定性与生物相容性矛盾 |
| （兜底无匹配） | 需进一步确认核心技术和市场风险 |

---

## §7 领域警告映射表（Warning Map，≥20 条）

在特定领域关键词命中时，强制附加警告（供第零章和第九章"坦诚最大不确定性"使用）。

| 命中关键词 | 强制警告 |
| :--- | :--- |
| 罕见病 | ⚠️ 罕见病研究数据稀缺、患者群体小，需建立专家网络 |
| 临床试验 | ⚠️ 临床试验证据周期长（3-7 年）、成本高，需充足准备 |
| FDA | ⚠️ FDA 审批要求严格、周期长，建议预留 12-24 个月缓冲 |
| NMPA | ⚠️ NMPA 医疗器械注册证据要求高（6-18 月），需提前合规布局 |
| 药物 | ⚠️ 药物研发知识链条长（平均 10-15 年），转化难度大 |
| 硬件 | ⚠️ 硬件与软件知识整合难度大，需跨学科团队协作 |
| 芯片 | ⚠️ 芯片设计知识门槛高（18-36 月学习曲线），需专家参与 |
| 传感器 | ⚠️ 传感器信号与生物学指标关联知识未完全建立，校准难 |
| 端侧推理 | ⚠️ 端侧模型精度可能低于云端，需在性能与隐私间取得平衡 |
| 知识图谱 | ⚠️ 知识更新滞后，需建立持续更新机制和领域专家审核流程 |
| 老药新用 | ⚠️ 老药新用机制理解不充分，临床试验证据仍有积累风险 |
| 绿色合成 | ⚠️ 绿色合成工艺基础研究不成熟，转化成本高于传统工艺 |
| 专利授权 | ⚠️ 专利知识布局周期长，需提前规划知识产权战略 |
| 预测性维护 | ⚠️ 工业场景知识复杂度高，数据质量参差不齐 |
| 能量采集 | ⚠️ 能量采集技术知识有限，适用场景受限 |
| 养老机构 | ⚠️ 跨学科合作网络建立周期长，知识验证迭代慢 |
| 小模型 | ⚠️ 小模型能力边界有限，需明确适用场景和性能边界 |
| 多模态 | ⚠️ 多模态融合知识复杂，研发周期长，标注成本高 |
| 模块化 | ⚠️ 模块间接口知识标准不统一，需定义清晰规范 |
| 材料 | ⚠️ 规模化生产工艺难度大，专利壁垒高 |
| 可穿戴 | ⚠️ 续航与舒适度矛盾，用户佩戴意愿低 |

---

## §8 红蓝旗热度标记（Heat Rules / Red-Blue Flags）

热度评估辅助标记：红旗=过热竞争领域（谨慎进入），蓝旗=高知识密度冷领域（优先整合）。供第零章"蒸馏优先级"和"知识密度"推断使用。

| 标记 | 关键词（≥10 条） |
| :--- | :--- |
| 红旗（过热） | 饮食、健康、健身、打卡、社区、社交、电商、外卖、SaaS、短视频、视频、博主、直播、大模型、LLM、Chatbot、生成式 AI、通用 AI、GPT |
| 蓝旗（高密度） | 生物、基因、靶点、合成、新材料、量子、纳米、免疫、细胞、传播、教育、心理、商业智能、数据分析、端侧推理、小模型、RAG、Agent、多模态 |

---

## §9 数据工具 → 证据类型 → 权重映射表

本表映射 11 个 SCP 专业数据工具（10 个 MCP 端点，单一事实源见 scp_tools.py TOOL_COUNT/ENDPOINT_COUNT）到其主返回证据类型与对应权重（权重取自 §1），供 L1 确定性计分层查表使用。AI 在调用 `scripts/scp_tools.py` 获取证据后，按本表确定证据类型，再交由 `scripts/score_evidence.py` 计分。

| 数据工具 | 主返回证据类型 | 对应权重 | §1 出处 |
| :--- | :--- | :--- | :--- |
| Origene-Search | Journal_article | 1.0 | §1 普通期刊论文行 |
| Origene-NCBI | External_validation | 2.5 | §1 外部验证行 |
| Origene-ChEMBL | Journal_article | 1.0 | §1 普通期刊论文行 |
| Scholar-KG | Systematic_review | 2.5 | §1 系统综述行 |
| Origene-TCGA | External_validation | 2.5 | §1 外部验证行（基因组数据复用） |
| Origene-OpenTargets | Meta_analysis | 2.5 | §1 Meta 分析行（靶点-疾病聚合） |
| Origene-FDADrug | FDA_NMPA_approval | 5.0 | §1 FDA/NMPA 批准行（最高等级） |
| Origene-PubChem | Journal_article | 1.0 | §1 普通期刊论文行（化学信息复用） |
| SciGraph-Material | Industry_standard | 3.0 | §1 行业标准行 |
| Sciverse | Patent | 3.0 | §1 专利文献行 |
| Semantic Search | Journal_article | 1.0 | §1 普通期刊论文行 |

**使用说明**：同一工具返回的多条证据可能分属不同子类型（如 Origene-Search 既可能返回 RCT 也可能返回综述），本表给出的是该工具的"主返回类型"作为默认映射；AI 在逐条识别证据类型时，若某条证据明显匹配更高权重的类型（如 Origene-Search 返回的某篇论文实际是 RCT），应按实际类型取权重，而非受限于本表的默认映射。这一规则与 §1"若一条证据同时命中多个类型关键词，取权重最高的那个类型"的原则一致。

---

## §10 证据权重精简表（7 档合并展示）
（迁移自 SKILL.md 原章节 3.1）

| 证据类型 | 权重 | 说明 |
| :--- | :--- | :--- |
| FDA_NMPA / CE_mark / RCT | 5.0 | 最高等级：药监认证或随机对照试验 |
| 行业标准 / 指南 / 专利 | 3.0 | 中等：行业标准或专利文献 |
| Meta 分析 / NCBI 数据库 | 2.5 | 中上等：汇总多项研究或权威数据库 |
| 系统综述 / PubMed 论文 / ChEMBL 数据 | 2.0 | 中等：学术论文或数据库记录 |
| 原型实现 / 普通综述 | 1.5 | 中下等：原型验证或综述文章 |
| 动物实验 / 病例研究 / 体外实验 / 算法基准 | 1.0 | 低等级：预临床或个案 |
| 模拟仿真 / 专家意见 / 新闻报道 | 0.5 | 最低等级：仅供参考 |
| 未识别类型（默认兜底） | 0.3 | 未命中上表任何类别时使用 |

精简表中的每一类证据类型都对应本章 §1 中的更细分子类，例如 FDA_NMPA 和 CE_mark 虽然合并为同一行，但在完整手册中各自有独立的关键词匹配列表；同理，动物实验、病例研究、体外实验、算法基准四个子类型在完整版中也是独立条目，只是权重相同所以在精简表中合并展示。AI 在进行证据类型匹配时，若遇到模棱两可的情形，应优先查阅完整手册的 §1 章节，按最匹配的子类型取值，而非简单套用精简表的大类。匹配时注意关键词的大小写不敏感，中英文混合匹配，例如同时接受「RCT」和「随机对照试验」作为命中关键词。若一条证据同时命中多个类型关键词，取权重最高的那个类型作为本证据的最终类型，不做多重加权。

---

## §11 置信度乘数（3 档）
（迁移自 SKILL.md 原章节 3.2）

| 置信度 | 乘数 | 判定方法 |
| :--- | :--- | :--- |
| high | 1.0 | 证据中同时包含 ≥3 种：样本量/患者数描述、统计指标（p-value / CI / 显著性）、明确结果结论、方法学步骤描述 |
| medium | 0.5 | 包含其中 2 种 |
| low | 0.25 | 仅含 1 种或完全无数据字段，仅有标题摘要 |

四类指标的判定细则如下，供 AI 在逐条检查时参考：样本量描述指标包括但不限于「n=」「N=」「患者数」「受试者人数」「样本规模」等明确的数量描述；统计指标包括但不限于 p-value、p<0.05、置信区间 CI、95%CI、统计显著性、显著差异、效应量、OR 值、HR 值等统计学术语；明确结果结论包括但不限于「结果显示」「结论表明」「显著优于」「非劣效于」「无统计学差异」「达到主要终点」「未达到终点」等对研究结果的明确陈述；方法学步骤描述包括但不限于「采用 RCT 设计」「使用双盲对照」「采用前瞻性队列」「方法学简述如下」「实验流程」「数据采集方法」「统计分析方法」等对研究设计或实验方法的具体描述。四类指标的判定采用「命中即计数」原则，只要摘要或正文中出现任何一类指标的关键词就算命中该类，不需要完整的统计报告，也不需要指标数值一定显著，只要出现相关描述即可。

---

## §12 年份衰减规则
（迁移自 SKILL.md 原章节 3.3）

证据年份距今超过 5 年时，每超出 1 年权重线性递减 0.05，衰减系数最低降至 0.3（即距今 14 年以上的旧文献统一按权重乘以 0.3 计算）。距今 5 年及以内的证据不执行衰减，系数保持为 1.0。计算公式如下：`decay_factor = max(0.3, 1 - max(0, years_old - 5) * 0.05)`。若证据条目中未提供具体发表年份，则默认不衰减（decay_factor = 1.0），但需在第十章证据索引表的有效性状态列中注明「年份缺失，衰减未执行」。年份计算的基准为当前系统日期的公历年份，例如当前为 2026 年，则 2021 年及以后发表的论文不衰减，2020 年衰减 0.95，2019 年衰减 0.90，依此类推，直至 2012 年及更早论文衰减至 0.3 后不再继续下降。若论文为在线优先发表（online first / epub ahead of print）状态，优先使用在线发表日期而非正式印刷版日期计算。年份信息若只提供了年份范围（如「2018-2020」），取其中值作为计算基准；若只提供了世纪年代（如「2010 年代」），则按该年代的最后一年（2019）计算，即取偏保守的衰减策略。

---

## §13 证据总分等级阈值
（满分 8.0，≥8.0 为强证据门槛；迁移自 SKILL.md 原章节 3.4）

| 等级 | 总分阈值 | 人话标签 | 含义 |
| :--- | :--- | :--- | :--- |
| 🟢 green | ≥ 8.0 | 强 | 证据充分，蒸馏结论可信度高 |
| 🟡 yellow | 4.0 ~ 7.99 | 中 | 证据中等，部分关键判断仍需补充 |
| 🔴 red | < 4.0 | 弱 | 证据薄弱，蒸馏结论仅供参考 |

三个等级对应的后续处理策略如下：green 等级的证据可以支撑大部分判断标注为【确证】，蒸馏简报的整体解释力度较强，可用于支撑正式决策；yellow 等级的证据可支撑中等置信度的判断，部分关键交叉点仍建议追加一轮定向文献检索或专家咨询；red 等级的证据仅能支撑【推断】级别判断，蒸馏结论参考价值有限，建议用户提供更多关键词或补充具体论文后重新执行蒸馏。总分等级仅用于蒸馏前的闸口检查和蒸馏后的置信度参考，是第九章三选一结论判定的输入之一，完整判定标准见 §14 三选一结论判定矩阵。yellow 等级是最常见的中间状态，绝大多数蒸馏任务都会落在这个区间，AI 不必刻意追求 green 等级。

---

## §14 三选一结论判定标准矩阵
（迁移自 SKILL.md 原章节 3.5）

第九章三选一结论（建议优先整合 / 建议持续追踪 / 建议暂时搁置 / 证据不足，无法给出方向性结论）不是凭 LLM 直觉选择，而是按以下判定矩阵确定。判定时按从严格到宽松的顺序逐条检查，命中第一条即定论，不再继续向下判断：

| 结论 | 判定条件（须全部满足） |
| :--- | :--- |
| 证据不足，无法给出方向性结论 | `score_evidence.py` 输出 `level=="insufficient"`（即 `max_core_base_weight` 低于核心证据门槛：全局 2.5，AI 域 1.5、硬件/材料域 2.0，见 §14.2），**无论总分多高**都直接命中此行，不得跳到下面的 green/yellow/red 分支 |
| 建议优先整合 | 证据等级 green（≥8.0）且有效论文 ≥3 条且第七章无未消解的核心冲突且第二章已识别出可执行的跨学科整合路径 |
| 建议持续追踪 | 证据等级 yellow（4.0~7.99）或有效论文 1~2 条或存在部分消解的冲突但交叉方向值得继续跟踪或**红区但有效论文 ≥3 条（权重低但有真实证据，常见于 AI 算法研究领域会议论文为主）**或**证据等级 green 但第七章存在未消解的核心冲突**（最常见于算法侧成熟、临床侧欠收的交叉点，如 DR 深度学习分类） |
| 建议暂时搁置 | 证据等级 red（<4.0）**且有效论文 <3 条**，或有效论文为 0（降级后），或第七章核心冲突无法消解且无任何可识别的整合路径 |

**insufficient 分支说明**（必须严格遵守）：
- `level=="insufficient"` 是 `score_evidence.py` 在 `max_core_base_weight < CORE_EVIDENCE_THRESHOLD (2.5)` 时直接返回的等级，**不再走"算高分再 cap 到 yellow"的旧逻辑**。
- 命中 insufficient 时，结论只能是「证据不足，无法给出方向性结论」，**严禁**套用 yellow 的"建议持续追踪"或 red 的"建议暂时搁置"。
- L0 卡片不显示三选一结论符号，改显示「⚪ 证据不足」+ 一句话理由（说明缺哪类核心证据）。
- 第九章"结论依据"必须引用 `threshold_note` 字段原文（如"max_base_weight=1.0<2.5"）。
- `validate_output.py` 的 `CONCLUSION_OPTIONS` 已含「证据不足，无法给出方向性结论」，命中此行时文本结论必须与该字符串完全一致。

若上述四条均无法明确命中（极少见的边界情形），默认采用「建议持续追踪」并在第九章坦诚中说明判定边界模糊的原因。快思维第零章给出的直觉结论若与本矩阵判定结果冲突，以本矩阵为准，并在第九章坦诚声明中披露这一分歧。本矩阵是第九章结论的唯一判定依据，取代任何其他章节中关于结论选择的描述。

### §14.1 与第九章强校验（L0/L1/L2/L3 全档位强制）

L0 卡片、L1 五块、L2 十章、L3 JSON 审计日志中的"结论"字段必须与按本 §14 矩阵判定的结果**完全一致**，不得有任何偏差。`scripts/validate_output.py` 会对以下强校验项硬失败：

1. **黄区不得写"优先整合"**：若 `score_evidence.py` 输出 `level=="yellow"`，第九章/L0 卡片结论只能是「建议持续追踪」或「建议暂时搁置」，**严禁**出现「建议优先整合」。违反 → 校验失败。（注：未达核心证据门槛的情形现在直接判 `insufficient`，不再 cap 到 yellow，见上一条。）
2. **红区 + 有效论文 <3 不得写"优先整合"或"持续追踪"**：若 `level=="red"` 且 `valid_evidence_count < 3`，结论只能是「建议暂时搁置」。**例外**：若 `level=="red"` 但 `valid_evidence_count ≥ 3`（权重低但有真实证据，常见于 AI 算法研究领域会议论文为主），允许写「建议持续追踪」，但必须在第九章坦诚中声明"红区但有效证据≥3 条，证据权重低但真实存在"。
3. **有效论文为 0（含全 mock）不得写"优先整合"**：`score_evidence.py` 输出 `rule2_triggered==true` 或 `valid_evidence_count==0` 时，结论只能是「建议暂时搁置」，且 L0 卡片顶部必须显示"模拟数据"红条，不得输出三选一结论（改显示"需你决策"）。
4. **L0 卡片结论字段 == L3 JSON `conclusion` 字段**：两档位输出虽渲染形式不同，但底层数据必须同源。校验器会比较两者的规范化字符串（去符号、去空白、小写）。
5. **绿区 + 有效论文≥3 + 无未消解冲突 + 有整合路径** → 必须「建议优先整合」；不得写「持续追踪」或「搁置」（除非用户显式要求降级并在第九章声明）。
6. **insufficient 不得套用三选一**：若 `score_evidence.py` 输出 `level=="insufficient"`（`threshold_note` 含 "insufficient: no core evidence"），结论只能是「证据不足，无法给出方向性结论」，**严禁**写"建议优先整合/持续追踪/暂时搁置"任一。L0 卡片不显示三选一符号，改显示「⚪ 证据不足」。违反 → 校验失败。

### §14.2 核心证据门槛与 §14 矩阵的联动

`score_evidence.py` 的核心证据门槛逻辑（全局 `core_evidence_threshold=2.5`，支持域级覆盖）：

**域级门槛理由说明**（新增域时按此逻辑确定阈值，P1-3）：

| 域 | 门槛 | 理由 |
| :--- | :--- | :--- |
| 全局默认 | 2.5 | 要求至少一条系统综述/Meta 分析/外部验证级别证据 |
| AI | 1.5 | AI 域以会议论文（域覆盖后 1.5）和基准测试为主要证据形态，无临床/监管硬证据；门槛设为 1.5 即"至少一条 AI 域会议论文" |
| hardware | 2.0 | 硬件域原型实现（域覆盖后 2.0）是功能验证的主要形态；门槛设为 2.0 即"至少一条原型实现或行业标准" |
| material | 2.0 | 同 hardware，材料域以原型实现和行业标准为主要证据形态 |

**新增域时的门槛确定流程**（详见 CONTRIBUTING.md「新增域 checklist」）：
1. 列出该域的主要证据形态（会议论文？原型实现？临床 RCT？）
2. 查 `domain_overrides` 该域的证据类型权重
3. 门槛 = 该域主要证据形态的权重（"至少有一条像样硬证据"的判定线）
4. 若无域覆盖，门槛 = 全局 2.5

- **域级门槛（P1-1）**：`evidence_weights.json` 的 `core_evidence_threshold_by_domain` 允许按域覆盖全局 2.5——当前 **AI=1.5、hardware=2.0、material=2.0**，域级值优先。设计理由：这些域以会议论文、基准测试与原型实现为主要证据形态，单条基础权重天然低于临床/监管域（AI 会议论文经域权重覆盖后 1.5、硬件原型实现 2.0）；若无覆盖，纯 AI 会议论文方向即使有 20 篇真实可验证文献也会永远判 insufficient。域级阈值只降低"是否有一条像样硬证据"的判定线，**不改变 green/yellow/red 的 8.0/4.0 分界**；临床/监管等其余域维持 2.5 不变。若这些域的全部核心证据仍低于域级门槛，判 insufficient 仍是预期行为——正确动作是补检系统综述/RCT/监管文件，而非继续降门槛。实际使用的门槛值随计分结果输出（`core_evidence_threshold` 字段），可审计。
- **insufficient 优先判定**：若 core 层的 `max_core_base_weight` 低于该域有效门槛，直接返回 `level="insufficient"`，`threshold_note` 含 "insufficient: no core evidence"。§14 矩阵按 insufficient 行判定 → 结论只能是「证据不足，无法给出方向性结论」。**此规则覆盖任何"总分≥8.0 应当 green"的直觉判断**。
- **旧"cap 到 yellow"逻辑已废弃**：不再出现"core_evidence_threshold_capped_to_yellow"字段。若 `threshold_note` 含 "insufficient" 前缀，等级就是 insufficient，不是 yellow。
- 若 core 层的 `max_core_base_weight >= 2.5`，正常走 green/yellow/red 分支，等级判定用 `total_core`（仅 core 层加权分），不用 `total_score`（含 supplemental）。

### §14.3 证据分层规则（tier 判定）

`score_evidence.py` 对每条证据打 `tier` 标签，分三层：

| 层级 | 判定条件 | 用途 |
| :--- | :--- | :--- |
| core | 同行评审原始研究/综述/**监管批准**，有 DOI + 有年份 + 非 mock + 非 duplicate + 非格式错误 + 非未来年份 + 非预印本 | 等级判定（total_core）+ 核心证据门槛（max_core_base_weight） |
| supplemental | 非批准类监管文件 / 预印本 / 待核验条目（DOI 未核验或未来年份） | 仅展示计分供审计，不计入等级判定 |
| reference | 缺 DOI 或缺年份的纯参考条目 | 不计入任何有效计数 |

**监管批准进 core**：`FDA_NMPA_approval`、`CE_mark` 是监管机构对临床有效性的正式确认，属于硬证据，进 core 参与等级判定。其他监管文件（警告、召回、非批准类公告）进 supplemental。
**注意**：监管批准进 core 后，单条 5.0 可使 `max_core_base_weight` 直接达标、`total_core` 显著上升，可能触发 green。若第七章存在未消解核心冲突，§14 矩阵的 green+冲突分支会将其结论固定为「建议持续追踪」，不会被误判为「优先整合」。
**预印本识别**（满足任一即判预印本）：`is_preprint=True`，或 DOI 以 `arxiv:` 开头，或 source 含 `arxiv.org`，或 DOI 含 DataCite 形式的 arXiv 标识 `10.48550/arxiv`（大小写不敏感；该形式 DOI 是预印本注册标识，不得按普通期刊 DOI 进 core）。
**未来年份识别**：year > current_year（支持 int / float / 数字型字符串，统一强转后比较）。

**等级判定流程**：
1. 遍历证据，按上述规则打 tier 标签
2. 累计 `total_core`（仅 core 层加权分）与 `total_supplemental`（supplemental 层加权分）
3. 检查 `max_core_base_weight`（仅 core 层的最大基础权重，含监管批准）
4. 若 `max_core_base_weight` 低于域级有效门槛（§14.2）→ insufficient
5. 否则按 `total_core` 判定：≥8.0 green / 4.0-7.99 yellow / <4.0 red
6. `total_score`（含 supplemental）仅作审计展示，不参与等级判定

---

## §15 手算示例
（迁移自 SKILL.md 原章节 3.6）

以一篇 2018 年发表的 RCT 论文为例（假设当前年份为 2026 年，故距今 8 年），论文数据中包含样本量描述、p-value 统计值和显著结果结论三项要素，符合 high 置信度判定条件。计算步骤如下：第一步，证据类型查表，RCT 对应的基础权重为 5.0；第二步，置信度为 high，对应乘数 1.0，相乘后得 5.0 × 1.0 = 5.0；第三步，年份衰减计算，years_old = 8 大于 5，超出年数为 3 年，decay = max(0.3, 1 - 3 × 0.05) = max(0.3, 0.85) = 0.85，乘以第二步结果得 5.0 × 0.85 = 4.25；第四步，该条证据的最终贡献得分为 4.25。若本次蒸馏仅此一篇论文，则证据总分为 4.25，对应等级为 yellow 🟡。再举一个多证据累加示例：假设另有一篇 2023 年（距今 3 年，不衰减）发表的专利文献，置信度为 medium，则该篇得分为 3.0 × 0.5 × 1.0 = 1.5；两篇合计总分 4.25 + 1.5 = 5.75，等级仍为 yellow 🟡，但整体证据强度有所提升。再追加第三篇 2024 年发表的系统综述、high 置信度：2.0 × 1.0 × 1.0 = 2.0，累计总分 7.75，仍然是 yellow，距离 green 的 8.0 只差 0.25，这种情况属于高 yellow，接近 green 边界，蒸馏结论的解释力度可适当向 green 靠拢，但标签等级仍应严格标注为 yellow，不得自行上调。

手算完成后，建议 AI 做一个快速的一致性复核。例如：最高等级的单条证据（RCT × high × 不衰减 = 5.0）就是理论上单条得分的上限，任何单条得分超过 5.0 都说明计算有误；同理，最低等级的单条证据（未识别类型 × low × 14 年以上 = 0.3 × 0.25 × 0.3 = 0.0225），任何低于此值的单条得分也说明有误。通过检查最大最小值边界，可以快速捕捉手算中的笔误或乘法错误，不需要逐项重算即可获得一定的质量保证。

---

## §16 常见手算错误清单
（迁移自 SKILL.md 原章节 3 常见手算错误清单）

以下列出 AI 手算证据得分时最高频的五类错误，建议手算后对照复核。错误一：年份衰减方向算反，距今越久分数应该越低，但有人误将 8 年前算成 +3 年，乘以大于 1 的系数导致分数越老越高。错误二：置信度乘数与年份衰减的顺序搞混，虽然乘法交换律下结果相同，但如果中间过程把乘数用在了错误的字段上容易出错，建议严格按 权重×乘数×衰减 的固定顺序计算。错误三：green 等级阈值记成 ≥7.0，正确是 ≥8.0，差 1 分的边界案例最容易出错。错误四：red 等级和强制终止阈值混淆，<4.0 是 red 等级但仍可蒸馏，<0.5 才是强制终止，两者差了 3.5 分的区间。错误五：多证据累加时最后一条忘记加上，导致总分总是比正确值少最后一篇的得分，建议手算完成后再数一下 evidence 数量和 detailed_scores 数量是否一致。
