---
name: intersci-kd
description: |
  跨学科知识蒸馏引擎。输入技术方向或研究课题，自动（或指导AI）检索文献、执行确定性证据
  计分、闸口检查，输出包含领域关联图谱、交叉点识别、证据审计的十章结构化简报与三选
  一决策建议。触发词："知识蒸馏"、"跨学科分析"、"证据锚定"、"研究简报"、
  "distill research"、"柔性传感器"、"血糖监测"、"AI 药物设计"、"跨学科图谱"、
  "交叉研究"。出口管制提示：芯片、加密、材料等受管制领域，用户需自行核实合规性。
license: MIT
tags: [knowledge-distillation, cross-disciplinary, evidence-based, research-analysis,
  decision-support, systematic-review, evidence-gap-analysis, research-priority,
  domain-mapping, conflict-resolution]
version: "4.6.1-skill"
author: InterSci-KD Team
created: "2025-01-01"
updated: "2026-09-15"
category: research
type: skill-engine
dependencies: "python3.8+, requests (scp_tools.py only)"
output_levels:
  default: [首轮仅输出 L0 卡片+定位问题，暂停等待用户选择，不自动展开]
  L0_卡片: [30秒决策卡片，结论先行；用户说"快速/只要结论"也走此档]
  L1_精简: [五块结构，3分钟阅读；用户回复"继续/精简/3分钟"后输出]
  L2_完整: [十章完整审计，含证据索引表；用户回复"完整/详细"后输出]
  L3_审计: [十章 + 计分明细日志，触发词：专家/审计/JSON]
---

# 使用快速参考卡（封面内页）

| 触发场景 | 核心约束 | 查阅 |
| :--- | :--- | :--- |
| 计分 | 禁止凭记忆编造权重/阈值，必须 Read 查表 | evidence-rubric.md §1/§10/§14 |
| 闸口 | score<0.5 → 暂停+A/B/C；有效论文为 0 → 全文【确证】→【推断】；估算偏差>1.5× → 硬终止 | 章节 4 |
| 三选一结论 | 「优先整合/持续追踪/暂时搁置」（或证据不足）不得模糊，按 §14 矩阵判定 | evidence-rubric.md §14 |
| 输出档位 | 首轮仅 L0+定位问题并暂停；"继续"→L1，"完整"→L2，"审计/JSON"→L3 | 章节 5 |
| 免责声明 | 「## 免责声明」；L0 一句话/L1 以上完整版；禁止指令性文字外泄 | output-template.md |
| 人话翻译 | 命中映射表即用；禁止内部术语泄漏（校验器 19/20 项拦截） | plain-language-map.md |
| 输出校验 | 交付前必跑校验器，--level 必填，硬失败=0 才交付 | scripts/validate_output.py |

## 核心执行约束

1. **首轮只出 L0 卡片**（含定位问题）并暂停等待用户选择，不自动展开；展开 L2 必须以「第零章：跨学科全景扫描」开篇，先直觉（≤40%置信度）后严谨。
2. **判断必须带置信度标签**（【确证】【推断】【无法判断】双轨），计分必须 Read 查 evidence-rubric.md，禁止凭记忆编造权重；**结论必须三选一**（优先整合/持续追踪/暂时搁置，或证据不足），按 §14 矩阵判定不得模糊。
3. **角色是执行引擎不是学者**：不输出哲学叙事，只输出协议规定的结构化结果；适用领域见 output-template.md，工作模式见章节 2。
4. **中间文件隔离**：计分/校验的中间 JSON/MD 写入系统临时目录（Windows %TEMP%\intersci-kd\，Unix /tmp/intersci-kd/）用完即删，禁止在工作区根目录创建；仅用户明确要求落盘的交付产物可保留。

# 章节 2：五种工作模式

「工作模式」决定**如何蒸馏**，与 frontmatter 的输出档位（L0~L3，决定输出多少）是两个独立维度。

| 工作模式 | 触发条件 | 执行要点 |
| :--- | :--- | :--- |
| 锚定蒸馏 | 提供 ≥1 篇具体论文 | 跳过检索 Top-10 过滤直接提取证据；≤2 篇建议补一轮检索 |
| 图谱生成 | 仅有方向描述（默认） | 必须执行文献检索（脚本优先/WebSearch 兜底），语义相关 Top-10 进计分 |
| 批量对比 | ≥2 篇独立论文或课题 | 每课题独立走 6 步协议，第十章附「对比结论索引表」 |
| 局部更新 | 含「更新/修改/调整/补充」 | 仅增量编辑指定章节；证据变化须重走闸口，措辞润色不触发 |
| 外部对标 | 「对标/竞品/基准」+外部参数值 | 第六章加「对标杆差距分析」，触发估算偏差闸口（章节 4 规则 3） |

模式判定优先级：局部更新 > 外部对标 > 批量对比 > 锚定蒸馏 > 图谱生成。

---

# 章节 3：证据评分体系

本节是证据计分的执行协议（指令），所有数值/表格外置，AI 必须严格遵守：

1. **禁止凭记忆编造权重或阈值**：必须 Read 查 evidence-rubric.md——权重 §1+§10、置信度 §2+§11、衰减 §3+§12、等级阈值 §3+§13、三选一矩阵 §14、工具映射 §9；一条证据命中多类型关键词时取权重最高者（最高 §1 RCT/FDA=5.0）。
2. **降级路径**：rubric 不可读 → 立即降级调用 scripts/score_evidence.py；脚本也不可用才允许手算，并在第九章声明「证据计分因参考文件和脚本双重不可用采用手算」。

3. **精简计分公式（仅用于速算自查，查表优先）**：单条证据得分 = 基础权重 × 置信度乘数（高 1.0/中 0.5/低 0.25）× 年份衰减因子（≤5 年 1.0，>5 年每年 -0.05，地板 0.3）。等级仅计 core 层合计：绿 ≥8.0 / 黄 4.0~7.99 / 红 <4.0；core 无单条基础权重达到核心证据门槛（全局 2.5；AI 域 1.5、硬件/材料域 2.0，单一事实源 evidence_weights.json）直接 insufficient。三选一结论矩阵见 evidence-rubric.md §14。

---

# 章节 4：闸口检查规则

六步协议第 4 步强制执行，顺序规则 1→2→3，前序已终止则后序跳过；触发终止立即停，触发降级必须声明替换标签，不得静默。

## 规则 1：证据极低检查点（score<0.5，暂停并交还决策权）

触发诊断性检查点，禁止继续生成简报，必须按以下三段式引用块输出诊断单：

> **🛑 证据极度匮乏（得分：{score}）**
> 当前检索未达到最低蒸馏阈值。作为您的学术副驾驶，我建议：
> A. **扩大检索**：放宽年份限制（5年→10年）或更换关键词，我来自动重试
> B. **人工锚定**：您提供 2~3 篇该方向的核心论文，我跳过检索直接进入蒸馏
> C. **强制推断**：基于我的训练数据生成低置信度（≤30%）的推断版简报（明确声明不依赖外部证据）
>
> 请回复 A / B / C 以继续。

A→扩年限+扩关键词重检索，仍<0.5 提示选 B/C；B→等用户贴论文后从第 3 步起；C→全文【推断】+第九章 ≤30% 声明。0.5≤score<1.0 触发降级：【确证】→【推断】+警告声明。

## 规则 2 与规则 3（有效论文为 0 降级 ／ 估算偏差终止）

**规则 2**：第十章「有效性状态」无一「有效」（含空检索/全 mock）→ 全文【确证】→【推断】+第九章必填声明；与规则 1 并列触发时标签只换一次但两处声明都要，仅精确「有效」二字计数。**规则 3（外部对标专属）**：|估算值÷基准值|>1.5 或 <0.67 → 硬终止，标准措辞：「无法判断。{参数名}比值{ratio}超1.5倍闸口，建议核实基准或重检索」；比值保留 2 位小数，基准为 0 跳过。

---

# 章节 5：执行协议（Agentic Protocol）

六步是内部 SOP，严禁用「第N步」作用户标题；用户输出严格遵循 output-template.md 十章结构，不得改序自创。协议内含 4 层推理模型：**L1 确定性计分**（章节 3+score_evidence.py）→ **L2 冲突检测**（第 3.5 步）→ **L3 闸口验证**（章节 4 三道闸口）→ **L4 结论综合**（§14 矩阵→第九章）。

**第 1 步 输入解析**：模式判定同章节 2 优先级；领域识别查 evidence-rubric.md §4 关键词（多领域查 §4.1 交叉表，冲突取频次高者，写第零章和第九章）；结构化字段优先，自然语言正则抽论文，整理 evidence 数组。

**第 2 步 文献检索**（图谱/批量/对标必执行，锚定 ≤2 篇建议补足）：①脚本优先 `python scripts/search_papers.py --query "{query}" --top_k 10 --domain {domain}`；②脚本不可用→WebSearch 兜底（2~3 组合查询）手动抽 ≥10 条；③关键词打分（标题×2/摘要×1）取 Top-10，选通用词+缩写+核心研究者名组合。**补检自动化**：结果全是会议/期刊论文且 Key 可用时自动追加补检（系统综述/Meta/RCT/FDA/临床指南），LLM 只需在第九章声明结果。

**第 3 步 确定性证据计分**：①类型识别→rubric §1 关键词取最高 type；②置信度→命中指标（样本量/p-value/CI/significant/methodology）≥3 类 high、2 类 medium、≤1 类 low，评级须克制（缺一类降一档，宁低勿高）；③年份衰减→章节 3 规则；④得分=权重×乘数×衰减，累加判定等级。脚本优先：`python scripts/score_evidence.py --input-json <文件> --domain <域>`（Windows/PowerShell 必须用 --input-json，禁用 --single；Unix 可用 --single）；严禁编造数值，脚本失败手算须声明，字段缺失按默认。

**第 3.5 步 证据冲突检测 + 第 4 步 闸口验证**：计分后闸口前两两比对，不阻断——结论方向相反且得分均 ≥1.0→核心冲突，数值区间无重叠→数据冲突，结果作第七章素材（未检出冲突须补 ≥1 条领域常识潜在冲突）；随后按章节 4 规则 1→2→3 顺序验证，多条件命中优先强制终止、多降级合并执行，通过后做十章结构规划。

## 生成结构化简报（默认仅 L0，按用户指令逐档展开）

**首轮只生成 L0 卡片并暂停**，不得自动连续渲染三档：L0（30 秒决策卡片，结论先行+定位问题+证据分数+最大不确定性+下一步，结尾给展开选项）→ 用户"继续/精简/3分钟"→L1 五块；"完整/详细"→L2 十章；"审计/JSON/专家"→L3（L2+计分明细日志）。定位回答（A 算法研究/B 临床部署/C 不确定）是 L1/L2 结论方向的输入，展开时按已确认目标重估结论，不沿用 L0 占位结论。三档数据同源：底层证据一次计分，三档是同一结果的三个视图。

**档位例外**：首轮"快速/只要结论"→仅 L0 暂停；"完整+方向/直接出报告"→可一次输出 L0→L1→L2（三档分别校验）；"精简"→跳过 L0 直接 L1；无关键词→默认仅 L0；用户输入含"分析/研究/详细/帮我看看"等显式要求实质内容的词→首轮可 L0+L1 连续输出（不暂停，L2 前仍暂停，P1-4 减少交互摩擦）。

### 无依据预测、指令收口与输出后自校验（全档位强制）

无引用的预测性判断必须标【推断】并注明"基于训练数据，未经检索验证"，或直接删除；机械升级承诺改为"补硬证据后重新计分"，不承诺时间；模板中对 AI 的指令文字（含 SYSTEM INSTRUCTIONS 区块）禁止渲染给用户。以上由校验器第 18/20 项拦截。

**输出 L0/L1/L2/L3 后必须自校验**：`python scripts/validate_output.py --input <输出文件> --level L0`（--level 必填；哪个档位被生成就校验哪个档位），硬失败必须重写后复检，0 硬失败才交付。校验器内置 21 项编号检查（13 为警告不阻断，16 仅 L0，20 指令泄漏与 21 计分签名全档位硬门禁），档位适用集由脚本自动适配，实现见 validate_output.py docstring。

**计分必须走脚本（P1-1 硬门禁）**：L2/L3 输出的 JSON 审计日志必须原样嵌入 `score_evidence.py` 的输出（含 `scored_by` / `scored_by_version` / `scored_at` 字段），不得手写——校验器第 21 项拦截签名缺失或非脚本来源的计分结果。LLM 手算路径只允许在脚本双重不可用时使用，且必须在第九章声明；手算结果无法通过 L2/L3 校验（`"scored_by": "manual"` 同样硬失败），只能降级到 L1 交付。

### 禁止内部黑话与人话翻译（全档位强制）

用户输出禁止任何内部术语（脚本名、文件名、配置键名、内部章节/规则编号、推理层代号、内部字段名），违反则输出无效（校验器第 19 项自动检测，单一事实源 internal_jargon.json）。技术表述按 plain-language-map.md 固定映射翻译成人话，未命中保留原文、禁止自由发挥；每写完一段自查一次。

### 三档输出格式与结论前置

三档输出格式（L0 卡片字段 / L1 五块 / L2 十章审计）全部以 output-template.md 为唯一事实源，SKILL.md 不再重复模板；格式违规由校验器自动拦截。结论前置与低证据处理（全档位强制）：结论前置：L0 第一行、L1 第一块、L2 第零章"先说结论"；结论一律来自等级矩阵判定（强证据+硬证据且无未消解冲突→优先整合；其余高分或红区+有效≥3→持续追踪；弱区+有效<3→暂时搁置），不得 AI 现编。低证据特殊处理：总分<0.5 或有效证据为 0 时不显示三选一，改显示「需你决策：A.扩大检索 B.提供论文 C.低置信度推断」，全 mock 时卡片顶部加红条「🔴 模拟数据」。

**证据表策略**：≤10 条全展，>10 条取 Top-10 加说明行（截断条目仍参与计分）。**参考表接入**：第零章查瓶颈红蓝旗（rubric §6/§8）；第七章查冲突源（§6/§7）；第九章查风险提示（§7）。

## 附加免责声明（6 步协议第 6 步）

简报末尾附加「## 免责声明」：L0 用精简版（一句话「本简报基于公开文献生成，不构成研究决策依据。」），L1/L2/L3 用完整版；禁止把对 AI 的指令性文字渲染给用户（校验器第 20 项拦截）。局部更新未涉及则保留原声明。交付前自查：章节顺序、标签完整、证据表无空列、免责位置正确。

## 执行异常处理（一线自救优先，仍失败再兜底告知用户）

| 触发条件 | 一线修复（AI 自救） | 仍失败兜底 |
| :--- | :--- | :--- |
| 检索 <3 篇有效论文 | 放宽年限（5→10 年）+扩关键词重检索 | 转人工锚定，第七章请求用户提供 2~3 篇论文 |
| 证据缺 DOI/PMID | 标「无法验证」，基础权重 ×0.3，继续 | 第十章标注+脚注说明仅供参考 |
| 计分脚本报错/超时 | 按 evidence-rubric.md §10~§16 严格手算 | 第九章声明手算误差 ±0.3，建议复验 |
| 闸口硬终止 | 无（规则固定不可绕过） | 按章节 4 标准措辞交还用户决策权 |

四类异常降级：①检索全失败→evidence 置空走规则 1 并告知；②脚本+手算均不可行→建议重试或精简证据；③发现严重矛盾→第七章追加第 3 条冲突+倾向持续追踪；④用户中途打断→已过闸口按局部更新，否则重走第 1 步保留检索增量。

# 章节 6：输出模板校验点

完整模板与各章必填要素见 references/output-template.md。自查顺序：首尾（第零章+免责）→核心（第九章三选一+第十章证据表）→中间章节。软参考：确证占比匹配等级（green 40-60%/yellow 20-40%/red 0%）。

**各章必查**：第零章含六要素（先说结论/三行引用块/图谱/模块预览 4 列表/≥2 假设/理由）；第一章元问题+≥2 学科；第二/三章各 ≥2 条路径/约束；第四五章假设与阶段带标签；第七章 ≥2 冲突+消解+搁置条件；第八章 3 个无时间验证动作+验收标准；第九章三选一；第十章 6 列证据表状态非空（空检索加兜底）；文末免责声明（L0 一句话/L1 以上完整版）。

# 章节 7：约束与反例（违反任一条→无效输出）

❌ 禁止：编造 DOI/PMID/引用（无来源标「无法验证」）；用 Step 1/2/3 作章节标题（L2 首章必须「第零章」，禁止前置寒暄）；常规表 >3 列或每格 >20 字（第零章 4 列模块预览豁免）；模糊化三选一结论；提问超过 30 字或附加解释。

✅ 必须：第十章有效性状态 6 枚举非空（非「有效」状态加提示语）；有效论文=0 时全文【确证】→【推断】并显式声明；连续 3 个判断带标签（纯描述句豁免）；首轮用 ≥3 种视觉锚点分层（加粗/列表/引用块/表格）；末尾「## 免责声明」位置在正文后，禁止指令性文字外泄。交付前双重自检：有没有做禁止的事？有没有漏做必须的事？存疑保守修正不冒险。

---

# 章节 8：辅助脚本使用说明

脚本均在 scripts/，fail-fast；search/score 零依赖纯标准库，scp_tools.py 需 requests。优先脚本取确定性结果，手算仅兜底。退出码 0=成功/2=参数缺/1=内部异常（降级手算须注明）；API Key 仅环境变量读，绝不硬编码。

**数据工具集（11 个 SCP 专业数据工具，10 个 MCP 端点，MCP Streamable HTTP 架构）**：单一通用 Key（SCP_HUB_API_KEY，请求头 SCP-HUB-API-KEY）认证全部工具。工具数量单一事实源：scripts/scp_tools.py 的 TOOL_COUNT=11 / ENDPOINT_COUNT=10（Sciverse 端点提供 search_papers + semantic_search 两工具），由 scripts/check_consistency.py 对账，文档禁止硬编码其它数字；完整端点表见 README.md，工具→证据类型映射见 evidence-rubric.md §9。选工具策略：生物医药→Origene-NCBI+ChEMBL；药监→FDADrug（权重 5.0 最高）；材料硬件→SciGraph-Material；专利→Sciverse；不确定→Origene-Search 宽搜；知识图谱→Scholar-KG。

**SCP 网关三态降级**：①success 正常返回；②empty_result 后端正常但无匹配（hardware/material 域自动 fallback 到 sciverse 学术检索），返回真实空列表不虚报；③mock_fallback 网关不可达/认证失败，返回 is_mock:true 兜底数据。mock **不计入有效证据、不进 core 层**：全部 mock（或有效证据为 0）时不得照常输出三选一，必须触发规则 2 全文降级或走 insufficient 处置并在第九章声明；仅当 mock 与真实结果混排且有 ≥1 条真实可验证 core 证据时才继续，mock 不参与等级判定。

## 脚本速查

| 脚本 | 调用要点 | 关键返回 |
| :--- | :--- | :--- |
| search_papers.py | --query/--top_k 10/--domain；有 Key 自动委托 SCP，无 Key 本地 mock | papers[]（rank/title/year/doi/type/confidence） |
| score_evidence.py | --input-json 文件批量优先；--domain 必加；Windows 禁 --single | total_core/level/valid_evidence_count/max_core_base_weight |
| scp_tools.py | --tool 名/--query/--top_k；Key 缺自动降级 mock | status=success/empty_result/mock_fallback |
| 决策指引 | ≥2 条证据必用脚本；脚本失败手算+第九章声明 | 优先级：脚本结果>公式手算>估算（仅兜底须声明） |

---

# 版本历史

完整修订日志见 [CHANGELOG.md](CHANGELOG.md)。当前版本 4.6.0-skill，与 pyproject.toml 一致（scripts/check_consistency.py 对账）。
